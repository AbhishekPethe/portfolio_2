'use client'
import React from 'react'
import { cards } from '../data'
import NewProjectComponent from '../components/NewProjectComponent'


const AllProjects = ({setCursorVariant , setCursorText}) => {
  return (
      <div className='flex flex-col gap-6 md:gap-10 text-white'>
          {/* {
              cards.map((each , i) => {
                  return (
                      <NewProjectComponent key={i} each={each} setCursorVariant={setCursorVariant} setCursorText={setCursorText} />
                  )
              })
          } */}
          pzdvjsdkvmp
          
      </div> 
  )
}

export default AllProjects